# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
# FastAPI can goi nay de doc UploadFile (multipart/form-data) - thieu no thi
# POST /api/v1/doses/{id}/photo loi ngay luc chay, khong loi luc import (chi
# phat hien khi co request that goi toi route dung UploadFile).
python-multipart>=0.0.12

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0
langfuse>=2.0.0


# Database
sqlalchemy>=2.0.0
alembic>=1.14.0
psycopg2-binary>=2.9.0

# Auth — auth-api that (TASK-010, api-contracts.md muc 1), thay rao tam
# X-Internal-Secret (backend/api/security.py). bcrypt PIN <4.1 - passlib
# 1.7.4 doc bcrypt.__about__.__version__ (bi xoa tu bcrypt>=4.1), khong
# tuong thich - xac nhan bang test that 2026-08-12 (AttributeError luc
# hash_password()).
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<4.1
python-jose[cryptography]>=3.3.0
email-validator>=2.2.0

# Scheduler - nhac lai escalation theo thoi gian (vong 2, chatbot-rag-design.md
# muc 13). SQLAlchemyJobStore (khong dung in-memory mac dinh) - luu job vao
# chinh Postgres dang co, tranh moi worker process tu chay 1 scheduler rieng
# khi deploy nhieu worker (chatbot-rag-design.md muc 13, quyet dinh 2026-08-09).
apscheduler>=3.11.0

# Vector Store — pgvector (extension trong PostgreSQL, xem ADR-0008), KHONG dung ChromaDB rieng
pgvector>=0.3.6

# Xu ly anh — nen/resize anh xac nhan lieu thuoc truoc khi luu + gui VLM
# (backend/services/photo_verification/image_processing.py). Chon Pillow
# (~3MB) thay vi opencv (~60MB) - dung dung tinh than "giu image nhe" da neu
# trong vlm_bridge.py, o day chi can resize/nen JPEG, khong can xu ly ma tran
# pixel nhu vlm_demthuoc/ (webcam).
Pillow>=10.4.0

# LLM client truc tiep (dung cho batch embedding script, Phase 3 - ngoai LangGraph)
openai>=1.54.0
tiktoken>=0.8.0
# Offline/pre-release RAG judge only; never imported by the Agent runtime path.
deepeval>=2,<3

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# Web Push (VAPID) cho nhac gio uong thuoc khi app da dong - backend/services/push.py
pywebpush>=2.0.0

# B-08 production enablement (2026-08-28): runtime nhan dien anh thuoc cho
# backend/api/drug_image_chat_routes.py. Truoc day co chu dich KHONG nam
# trong requirements.txt (xem requirements-drug-image-vision.txt) - gio dua
# vao day vi DRUG_IMAGE_CHAT_RECOGNITION_ENABLED se duoc bat that qua bien
# moi truong Railway (mac dinh trong code van la false). Pin dung phien ban
# CPU-only da test o B-04/B-05. pytesseract la adapter Python; Tesseract va
# cac language pack vie+eng duoc cai trong production stage cua Dockerfile.
# OCR chi corroborate visual Top-1 va khong tu dong xac nhan thuoc.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu
open_clip_torch==2.26.1
pytesseract==0.3.13
