opencv-python>=4.8
numpy>=1.24

# Chỉ cần SDK của nhà cung cấp bạn dùng — cài cả hai cũng không sao.
openai>=1.40        # provider = openai (OpenRouter, Groq, Together, Ollama, LM Studio...)
anthropic>=0.72     # provider = claude
